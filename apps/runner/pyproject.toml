[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gpt-trace-runner"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.28,<1",
  "mcp>=1.28,<2",
  "cloakbrowser>=0.5,<1",
  "playwright>=1.53,<2",
  "pyarrow>=21,<22",
  "pydantic>=2.10,<3",
  "pydantic-settings>=2.7,<3",
  "rich>=13.9,<15",
  "typer>=0.15,<1",
]

[project.optional-dependencies]
test = ["pytest>=8.3,<9", "pytest-asyncio>=0.25,<1"]

[project.scripts]
gpt-trace = "gpt_trace_runner.cli:app"

[project.entry-points."gpt_trace_runner.benchmark_adapters"]
gaia = "gpt_trace_runner.superbench.adapters.gaia:GAIAAdapter"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
