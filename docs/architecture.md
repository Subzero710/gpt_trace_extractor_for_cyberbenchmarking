# Architecture

See [Kali Workstation runtime](workstation.md) for the trusted control plane, libvirt broker, per-attempt KVM boundary, AF_VSOCK guest agent, desktop/browser/shell coherence, identity recovery, network policy and operational lifecycle.
